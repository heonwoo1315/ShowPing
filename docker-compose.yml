services:
  redis:
    image: redis:7.0-alpine
    container_name: redis
    ports:
      - "6379:6379"
    networks:
      - showping-net
    deploy:
      resources:
        limits:
          memory: 64M

  # Zookeeper: 3.9 계열 중 가장 최신을 자동으로 가져오는 태그
  zookeeper:
    image: bitnami/zookeeper:3.9
    container_name: zookeeper
    environment:
      - ALLOW_ANONYMOUS_LOGIN=yes
    networks:
      - showping-net
    deploy:
      resources:
        limits:
          memory: 128M

  # Kafka: 3.9 계열 중 가장 최신을 자동으로 가져오는 태그
  kafka:
    image: bitnami/kafka:3.9
    container_name: kafka
    ports:
      - "9092:9092"
    environment:
      - KAFKA_BROKER_ID=1
      - KAFKA_CFG_ZOOKEEPER_CONNECT=zookeeper:2181
      - KAFKA_CFG_LISTENERS=PLAINTEXT://0.0.0.0:9092
      - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092
      - ALLOW_PLAINTEXT_LISTENER=yes
    depends_on:
      - zookeeper
    networks:
      - showping-net
    deploy:
      resources:
        limits:
          memory: 256M

  kms:
    image: kurento/kurento-media-server:latest
    container_name: kms
    ports:
      - "8888:8888"
    networks:
      - showping-net
    deploy:
      resources:
        limits:
          memory: 256M

  showping:
    image: ${DOCKER_USERNAME}/showping:latest
    container_name: showping
    ports:
      - "8080:8080"
    env_file: .env
    depends_on:
      - redis
      - kafka
      - kms
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_BOOTSTRAP_SERVERS=kafka:9092
    volumes:
      - /home/ec2-user/video:/app/video
    networks:
      - showping-net
    deploy:
      resources:
        limits:
          memory: 300M

networks:
  showping-net:
    driver: bridge